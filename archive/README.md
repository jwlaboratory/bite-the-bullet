# Archive

This directory keeps useful history out of the active runnable path.

- `legacy-root-experiments/`: older utility-gate files that previously sat
  directly under `experiments/`.
- `paper-experiments/`: planning notes that predate the current clean
  experiment set.
