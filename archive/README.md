# Archive

Old BTB work kept for provenance. **This is history, not the current claim** —
the active claim is [`../experiments/3-early-rdma/PAPER.md`](../experiments/3-early-rdma/PAPER.md).

The scripts here predate the current layout and are **not expected to run**
(their `parents[1]` path assumptions broke when they were archived).

## Buckets

- `prototypes/` — the earliest scripts and result dumps
  (`bite-the-bullet/`, `legacy-root/`).
- `superseded-experiments/` — experiment families dropped from the active claim:
  partial-prefix warming (`partial-prefill/`), older paper-grade synthetic runs
  (`clean-experiments/`), and paper-planning notes (`paper-experiments/`).
- `old-results/` — old utility-gate JSON outputs and model/hardware sweeps.
- `notes/` — literature reviews and broader routing research notes.
