# Bursted-ART BTB Results

Minimal paper artifacts:

- `PAPER.md`: short paper memo.
- `results.json`: raw `early_rdma` results only.

Main claim: predict sustained prefix reuse, then RDMA-copy existing KV into HBM before later requests arrive.
